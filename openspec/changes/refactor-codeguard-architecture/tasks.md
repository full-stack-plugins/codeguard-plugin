# Tasks

## 1. 基线与设计

- [x] 1.1 核对源码/远端/既有规格与未完成 change；基线 61e2592、275 单测（4 skipped）和 ruff 已实际执行。
- [x] 1.2 建立单一 proposal/design/specs/tasks；通过本 change 的 strict 校验后开始实现。
- [x] 1.3 用户明确要求 CodeGraph 分析；将当前工作树复制到临时目录建立索引（123 文件、1,795 节点、4,069 边），查询 CLI/MCP、Git hook、执行器、Dockerfile 与基线调用链。源仓未初始化索引；图谱是本次临时快照的静态证据。
- [x] 1.4 建立全能力保留矩阵（CLI/MCP/hooks/注册表/Java/CVE/快照/修复/配置/状态/发布），每项关联具体行为测试与当前边界；见 verification.md，不把已有覆盖当完整验收。

## 2. 执行与判定内核

- [x] 2.1 先用真实进程复现跨入口编码/权限/超时诊断丢失，再实现单一执行器；目标测试与全量测试通过（284 tests，保留 4 个历史 skip）。
- [x] 2.2 CLI lint/fix、PostToolUse、Git gate、CVE 主要检查执行迁移到统一进程边界，另覆盖 Dockerfile；保持旧导入/tuple 协议，入口契约与真实 MCP 回归通过。Git 二进制/探活/基线进程在对应基础设施任务中处理。
- [x] 2.3 结果/判定成为无宿主依赖的纯内核，既有判定用例逐项通过；架构依赖测试阻止反向导入（models/verdict 隔离导入、AST 门禁与真实反例）。
- [x] 2.4 检查计划和执行编排统一，显式保留 delta/repo/save 策略差异；第二文件失败、Java 多命令/环境与同范围复检用例通过。Git 逐文件基线豁免仍为应用层策略。

## 3. 门禁与语言适配器

- [x] 3.1 将 Git 意图解析与 hook 协议分开，纯 git_syntax 与仓库观察 git_context 独立；共享间接脚本读取、只解析一次模式，保留原导入面与预测暂存/推送/逃生门/多仓行为，真实 Git 与命令链回归通过。
- [x] 3.2 gate 应用、gate_checks 独立语言结果、repository_policy 安全/豁免、baseline、spec_validation 与 reporting 分离；gate_lib 为兼容导出。隔离运行、原仓/会话审计、并发保序、未知不获基线豁免实测通过；真实 argv 检查同时修复 scope 对非 Shell 参数的错误注入。
- [x] 3.3 registry_schema/registry/config/discovery/toolchain 分离，旧导入兼容；探活改为显式目标目录与单批次成功去重，失败重试/跨批重探/并行隔离/宿主 stdin/编码实测通过。坏配置不再静默默认或绕过自动修复，CLI/MCP 返回 UNVERIFIED；深层/嵌套根配置、57 项注册表与全量回归通过。
- [x] 3.4 Java 描述读取、纯反向闭包、纯命令策略、环境/版本差异观察与应用装配分离；默认等级、wrapper、动态全量、权威命令及环境注入回归通过。真实 Git 复现并修复版本误降级、嵌套基线和路径归属，Java 1.8/属性引用 JDK 定位修复；新增 21 项边界测试，全量 375 项（3 个历史 skip）。
- [x] 3.5 CVE 纯报告/判定、扫描 IO、生态编排与 CLI 分离；真实进程复现并修复坏报告/矛盾统计/非法分数/缺严重度误判，修复结果与复扫保留。新增 30 项边界测试；全量 405 项（3 个历史 skip）、142 项回归通过；在线漏洞服务与真实工具版本另行验收。

## 4. 状态与协议

- [x] 4.1 统一原子状态读改写、审计及已完成事件边界；6 进程 210 次统计/审计、并发消费 240 次计数、真实会话/worktree 与未知重试测试通过。硬门禁重复拒绝仍为 2，放行不缓存；旧无 ID 状态明确不保证隔离。Windows 锁实现未实机验收；内容/配置缓存继续 4.2。
- [x] 4.2 分离 fingerprint/cache，绑定 worktree/index 内容/upstream/文件内容/配置/命令；真实等长同 mtime 修改、linked worktree、未知恢复、配置更新和硬门禁不复用软缓存等 16 项通过。输入变化/预算超限不写缓存；软观察不是原子快照，外部动态输入与大型仓库性能边界已记录。
- [x] 4.3 CLI/MCP/五个 hooks 成为协议适配入口，保留用户可见行为；语言检查归 `language_check`、请求策略归 `check_application`，五类 Hook 分别委托 startup/session/prompt/git_guard/save 应用服务。独立导入、真实会话/Git、宿主 JSON 与 fail-open 回归通过；旧 Python 导入继续兼容。仍不把 CLI/MCP/Hook 的本地契约测试称为三宿主安装验收。

## 5. 质量与交付

- [x] 5.1 增加自动架构依赖/循环检查并接入 CI，临时源码树注入反向依赖/函数内导入/相对导入环后确实拒绝；本地已验证，远端 CI 待发布阶段。不以文件行数当合格标准。
- [x] 5.2 四个历史 skipped 已全部恢复；Dockerfile CLI 接入统一报告解析与扫描应用，14 项真实进程边界测试通过；基线比较按诊断内容和次数核验并要求基线实际 FAIL，三项误豁免用例先红后绿。完整 433 项单测零跳过、142 项回归零失败。
- [x] 5.3 新增当前架构 Mermaid 与扩展指南；报告版本读取插件 manifest，双语 README 指向当前证据，旧设计标明历史范围；双语标题/链接/版本及本地链接存在性测试通过。发布版本仍需在 5.5 检查。
- [x] 5.4 全量单测/真实 hook 回归/ruff/语言 schema/vendor 离线与在线/本 change strict 和逐条规格追溯通过；第十九批 495 项单测、143 项真实 Hook 回归、57 项/11 规则，证据见 verification.md。仅证明本地契约，不等同三宿主现场验收。
- [ ] 5.5 按仓规升级版本、生成市场元数据、授权的 PR/CI/不可变 tag/Release 闭环；v0.14.4 阶段版本已完成插件/市场双仓合并、PR/main CI、tag/Release，但第十九批新改动仍需独立发布。宿主安装运行独立记录，未授权不得改宿主。
- [ ] 5.6 完成全目标审计，代码/规格/证据一致后同步并归档 change，才可将整体优化标记完成。
